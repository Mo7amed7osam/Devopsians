apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: devopsians
type: Opaque
stringData:
  # MongoDB Atlas connection
  MONGO_URL: "mongodb+srv://mohaamedtariq12_db_user:wuiAicRs8kWVej08@cluster0.pdmzujy.mongodb.net/devopsians?retryWrites=true&w=majority&appName=Cluster0"
  
  # JWT / auth settings
  JWT_SECRET_KEY: "devopsians-super-secret-key"
  JWT_EXPIRE: "1d"
  COOKIE_EXPIRE: "1"
  
  # Production mode
  NODE_ENV: "production"
  
  # Frontend URL with STATIC IP (for CORS and Socket.IO)
  FRONTEND_URL: "https://34.228.5.183"
  
  # CORS Configuration - Allow all origins when behind ingress
  # The ingress controller handles external access control
  CORS_ALLOW_ALL: "true"
  
  # Cookie Security - Set to false if not using HTTPS
  # If you have TLS configured on ingress, change this to "true"
  SECURE_COOKIES: "false"
