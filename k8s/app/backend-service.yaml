apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: devopsians
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3030        # service port inside cluster
      targetPort: 3030  # containerPort in the pod