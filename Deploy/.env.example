# ==================================
# Devopsians Environment Configuration
# ==================================

# ============ ENVIRONMENT ============
# Options: development, staging, production
DEPLOY_ENV=production
NODE_ENV=production

# ============ DOCKER REGISTRY ============
DOCKERHUB_USERNAME=your-dockerhub-username
IMAGE_TAG=latest

# ============ PORTS ============
FRONTEND_PORT=80
BACKEND_PORT=3030
MONGO_PORT=27017

# ============ DATABASE ============
# MongoDB Root Credentials (for local MongoDB)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=adminpassword
MONGO_DATABASE=devopsians

# Full MongoDB Connection URL
# For local MongoDB:
# MONGO_URL=mongodb://admin:adminpassword@mongodb:27017/devopsians?authSource=admin
# For MongoDB Atlas (Recommended):
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=Cluster0

# ============ BACKEND CONFIGURATION ============
BACKEND_HOST=0.0.0.0
LOG_LEVEL=info

# ============ JWT CONFIGURATION ============
# IMPORTANT: Change this secret in production!
JWT_SECRET_KEY=change-this-secret-in-production
JWT_EXPIRES=1d

# ============ FRONTEND CONFIGURATION ============
# Frontend URL (where the frontend is hosted)
FRONTEND_URL=http://localhost

# API URL (backend endpoint for frontend to connect)
VITE_API_URL=http://localhost:3030

# ============ EMAIL CONFIGURATION (Optional) ============
EMAIL_USER=
EMAIL_PASS=
