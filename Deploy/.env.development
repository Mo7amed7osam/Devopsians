# Development Environment Configuration
DEPLOY_ENV=development
NODE_ENV=development

# Docker Configuration
DOCKERHUB_USERNAME=devopsians
IMAGE_TAG=dev

# Ports
FRONTEND_PORT=3000
BACKEND_PORT=3030
MONGO_PORT=27017

# Database
MONGO_ROOT_USERNAME=devadmin
MONGO_ROOT_PASSWORD=devpassword
MONGO_DATABASE=devopsians_dev
MONGO_URL=mongodb://devadmin:devpassword@mongodb:27017/devopsians_dev?authSource=admin

# Backend
BACKEND_HOST=0.0.0.0
LOG_LEVEL=debug

# JWT (Dev keys - not for production!)
JWT_SECRET_KEY=dev-secret-key-not-for-production
JWT_EXPIRES=7d

# Frontend
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3030

# Email (Optional)
EMAIL_USER=
EMAIL_PASS=
